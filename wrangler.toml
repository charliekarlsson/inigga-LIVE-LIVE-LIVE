name = "inigga-live-live-live"
compatibility_date = "2025-12-28"

# Cloudflare Pages configuration
pages_build_output_dir = "."

# Uncomment to deploy to a specific branch via wrangler pages deploy --branch
# branch = "production"

# For custom domains, configure them in the Cloudflare dashboard after first deploy.
